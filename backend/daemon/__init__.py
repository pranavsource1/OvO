"""
OVO Daemon — Background Audio Capture
───────────────────────────────────────
Zero-friction audio listener that captures musical ideas
from the microphone using Silero VAD for activity detection.

Run with:
  cd backend
  python -m daemon.listener

Requires optional dependencies:
  pip install sounddevice torch scipy
"""
